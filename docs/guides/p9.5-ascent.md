# P9.5 spec — The Ascent

**Phase closed 2026-08-01 (P9.5-T8).** This document began as the forward-looking
build spec; every unit section below is now reconciled against what actually
shipped — walked against the real commit history (`6f63b2e..e695670`, plus this
closing unit) rather than trusted from memory. Where a plan item shipped exactly
as written, the section says so plainly instead of silently staying ambiguous.
Where it diverged, the divergence and its reason are recorded inline, not
smoothed over. For the full session-by-session narrative (live-verification
detail, exact test counts, incident writeups), see `docs/STATUS.md`'s per-unit
P9.5 entries — this file stays the compact, unit-by-unit plan-vs-shipped record.

Read alongside: `CLAUDE.md`, `docs/guides/agent-playbook.md`,
`docs/guides/design-system.md`, `docs/DECISIONS.md` (D2, D3, D6, D9, D13, D14, D16
— D16 landed at T0, no longer a forward reference). UI units load
`.agents/skills/emil-design-eng` before JSX and apply the `review-animations`
standard before commit. The board-approved plan this spec mirrored lived in the
session plan file; this document is the repo-durable truth.

## Mission

Board round 1 on the P9 storefront: directionally great, far from Resend-level. This
phase closes that gap across every marketing surface, fixes two real auth bugs, and
builds the trust layer of an established product: feature pages, docs ascent,
changelog, status, blog, help — plus the open-source pack. **Pacing mandate: scope
never dilutes per-section polish; chunks ship when they meet the bar.**

**Delivered.** Every named surface shipped: T0's two auth bugs, T1-T3's system +
landing rebuild, T4/T5's supporting surfaces and docs ascent, both T-F feature-page
chunks, T6's changelog/status/OSS pack, T-R's blog and help center, T7's product
quick wins, and this T8 close. The pacing mandate held — three landing chunks and
two feature-page chunks each got their own board review before the next began,
matching the cadence the board locked below. What did NOT happen, on purpose: see
`docs/STATUS.md`'s P9.5 closing entry for the full "descoped / deferred / not this
phase" list (the deep studio/codes redesign, Stripe/P8.5, legal counsel review).

## Board decisions locked (2026-07-30)

- **Open source: approved. MIT.** README-as-marketing, SECURITY.md (hello@qrcdn.com),
  minimal CONTRIBUTING ("built in the open, developed as a cathedral"), Built-in-the-
  open landing section, repo links. P10's "flip private" item is REVERSED. Canon line:
  *if we ever disappear, the path off is public.*
- **Comparison: archetypes now** (free generators / shortener add-ons / enterprise
  platforms), named-vendor /compare pages post-launch with sourced claims.
- **Blog byline: Will Gibson, company-forward voice** — QRCDN is the entity; no
  solo-founder/build-in-public framing anywhere.
- **Two-register copy principle:** plain-language benefit headline (owners/execs) +
  mono technical proof-strip (developers), every section, devtool-esque theme.
- Cadence: artifact gate (board picks) → chunked builds, each board-reviewed on
  production before the next begins.

All five held for the full phase — none reopened or reversed mid-phase.

## Standing constraints

- Marketing routes stay `○ (Static)` — audited every unit.
- Entitlement numbers render only via `lib/entitlements.ts`/`lib/pricing.ts` imports.
- Print-truth rule: instrument-bearing QR surfaces stage on their own paper mat.
- Motion: tokens only; max one auto-advancing element per viewport; reduced-motion
  paths mandatory.
- Honesty: no fake metrics/logos/testimonials; blog/help/changelog describe only real
  work and behavior; comparison rows sourceable to our docs or category-common facts.
- **`supabase config push` is banned** (D16): the committed `[auth]` block is
  stale/local (`site_url=localhost`); the dashboard is authoritative. No dry-run
  exists — a push would clobber production auth.

Verified at T8's honest-content mechanical pass: zero fake metrics/logos/
testimonials/invented customers anywhere in rendered copy, and every marketing
route stayed `○ (Static)` through the final build. The entitlement-numbers
constraint had two small real violations, both found and fixed in the same pass —
`components/studio/create-code.tsx`'s free/pro dynamic-code-limit message and
`components/studio/kit-bar.tsx`'s free-kit-limit message both hardcoded the numbers
`PLAN_LIMITS` already carried, in files that (in `create-code.tsx`'s case) already
imported it for other copy on the same page. Fixed to interpolate from
`lib/entitlements.ts` directly; see this unit's report for the rendered text
(unchanged — same numbers, now sourced).

## Units (sequence: T0 → T1 → T2 gate → T3a→b→c each reviewed → T4/T5 → T-F ×2
reviewed → T6 + T-R → T7 → T8 close)

Real shipped order matched this sequence exactly, with T1 and T-F each splitting
into sub-commits the plan already anticipated (T1's own "Part A / Part B" split;
T-F's own "two reviewed chunks"): T0 (`8cfe8c0`) → T1a ×3 → T1b → T2 (no commit) →
T3a → T3b ×2 → T3c ×3 → T4 → T5 → T-F1 → T-F2 ×2 → T6 → T-R ×2 → T7 ×3 → T8 (this
unit). Full SHA list per unit below.

### T0 — Auth fixes (P0, first)
Diagnosis (read-only): dashboard email-template shape · SMTP settings (D9 says Resend
from day one; verify) · auth logs for the failed sign-up (rapid `otp_expired` pairs =
scanner fingerprint; GoTrue can't distinguish expired from consumed) · redirect
allow-list. Note: `signInWithOtp` creates users at SEND time — the reported address
likely exists half-created; handle deliberately.
Fix regardless: template → `{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=email`
(browser-independent; `email` not deprecated `magiclink`) · `/auth/confirm` becomes
page + `"use server"` `confirmSignInAction` + auto-submit island — a bare GET renders
the interstitial and consumes NOTHING; the visible "Confirm sign-in" button is the
no-JS fallback; `force-dynamic` with the safety comment · `/auth/callback` untouched ·
error flags `auth_error=link_invalid|oauth_failed` with distinct copy (CEO-final) ·
sent-state resend with countdown · e2e: money-path evolves (type=email + interstitial
assertion), new `auth-scanner-safety.spec.ts` (request.get must not consume; same
token then completes via page flow) · D16 entry + config.toml header comment.
Session-aware chrome: `hooks/use-session-affordance.ts` (signed-out first paint,
cookie-presence regex fast-path, `getClaims()` truth, `onAuthStateChange`) +
`NavAuthSlot` replacing both CTAs with "Open studio →" when authed (desktop + mobile).
`/login`: authed → `redirect("/studio")`; dual-mode copy (no tabs — backend is
identical either way); metadata retitle; noindex stays.

**Shipped exactly as planned (`8cfe8c0`).** The diagnosis's own root cause turned out
narrower than "the mail scanner did it": the dashboard's magic-link template was
using the Supabase default `{{ .ConfirmationURL }}`, which `/auth/confirm` never
matched at all (it reads `token_hash`/`type` query params) — every templated email
failed by construction, invisible to e2e because `generateLink` bypasses the template
entirely. Both fixes shipped anyway, since the scanner-consumption bug was real and
independent: a bare GET now renders and consumes nothing, proven by
`auth-scanner-safety.spec.ts`. One item folded in beyond the read-only diagnosis
list: `next` is now validated as a same-origin path in `/auth/confirm`, closing an
open-redirect the prior route had via an unvalidated `new URL(next, origin)`. No
other divergences.

### T1 — System
Part A defects: Recharts → hand-authored SVG area on the landing (design-guide rule) ·
hero entrance → CSS keyframes, `animation-fill-mode: backwards`, SSR ships full
opacity (e2e guard: served `<h1>` contains no `opacity:0`) · ScanNetwork md/lg →
100%-scale two-destination layout (today's chips render at 7.3px there).
Part B: additive `@theme` families in `globals.css` — fluid spacing
(`gutter/section-tight/section/section-air/block`), containers
(`page/copy/lede/docs/prose`), type scale (`display/h1/h2/h3/lede/eyebrow/code`),
`--code-*` colors — **D13 amendment lands in the same commit**; verify Tailwind v4
named-scale utility generation first (fallback: arbitrary-property syntax).
`components/marketing/section.tsx`: `Section` (variants stack/split/centered/showcase/
band · rhythm tight/standard/air · surface default/tint/floor · divider
hairline/fade/none) + `SectionHeading` (eyebrow/index/title/lede/aside/actions) +
`SectionBody`. Rules: dead-measure ban (narrow heading requires aside or full-measure
visual below) · centered ≤3/page · hairlines only between same-surface neighbors.
`Eyebrow` ordinal `index`; `Reveal` `trigger` prop.
Shiki: `lib/highlight.ts` (`import "server-only"`), JS-regex engine, grammars
bash/jsonc/ts individually, `lib/code-theme.ts` themed via `var(--code-*)`
(`colorReplacements` sentinel fallback — snapshot-test the emitted HTML), `CodeBlock`
async server + `CopyButton` island. Verify: no shiki client chunk.
Pilot: `/developers` structure — `lib/api-reference.ts` (typed endpoints, stable ids,
lang metadata; ERRORS relocated; "10,000" → `PLAN_LIMITS` import), helpers extracted
to `components/marketing/developers/`, sticky scroll-spy TOC island, page measure +
docs grid. Single route; split trigger ~15–20 endpoints.

**Shipped as two commits per the plan's own "Part A / Part B" split, not one T1
unit: T1a (three commits — `029e66c` Recharts→SVG, `32dc174` hero LCP CSS entrance,
`f964b79` ScanNetwork md/lg compact stage) then T1b (`ac2495f` — tokens, Section
primitive, shiki pipeline, `/developers` pilot).** Every listed item shipped;
`colorReplacements`/hex-sentinel was the planned fallback for shiki's theme and
turned out unnecessary — shiki's normalizer accepts literal `var(--code-*)` strings
directly (verified empirically against the installed version, pinned by
`lib/highlight.test.ts`'s snapshot so a future shiki upgrade that changes this
behavior fails loudly). **One self-flagged dependency divergence:** shiki shipped
pinned to `3.23.0`, the latest 3.x line, not the 4.x already on npm at build time —
recorded as a deviation in the T1b commit message itself, not silently taken. T1a's
production-build measurement: the landing's referenced JS for `/` dropped from
2,073,354 to 1,732,757 bytes (~16%) from the Recharts removal alone.

### T2 — Artifact gate (Fable-authored; board picks)
A1 hero mobile ×3 (spine / switchboard [doubles as RetargetTheatre] / orbit) + desktop
md/lg fix vs today · A2 landing storyboard 1440+390 — full 13-section IA incl.
comparison + built-in-open, surface alternation, numbered eyebrows, heading copy v2 ·
A3 code/console treatment · A4 pricing treatment (price-line / single-card-toggle /
pair). Copy decks drafted alongside.

**No code commit — a review/decision gate, exactly as planned, not a build unit.**
What's traceable in git history: A1 resolved to the orbit direction over a dial
alternative (T3a's commit message cites "the A1-R2 orbit-dial reference artifact" by
name), implemented at T3a as `components/marketing/orbit-stage.tsx` and carried
forward through T3b's board-round-5 device fixes. Copy deck v3 — including the
standing no-em-dash rule first documented at T3a — also originates from this review
cycle. A2/A3/A4's specific chosen variants are not separately recorded outside the
builds that resulted from them (T3a-c's section IA, T3c's API console, T4's pricing
matrix): this file can trace what those units shipped, not a standalone artifact
decision log for each round, so it says exactly that rather than inventing an A2-A4
outcome summary that doesn't exist in the repo.

### T3 — Landing v2 (three chunks, each board-reviewed on production)
a: system migration + hero pick + how-it-works + closing CTA.
b: playground polish (preset shelf animating controls · live contrast meter off
`worstContrast` · wider ink-hued bloom · single Download w/ format popover + success
check · NO added controls) · brand-system "one kit, many artifacts" per pick ·
RetargetTheatre + truthful `/u`·`/p`·expired state-cards · analytics enrichment (real
breakdown rows, live-today tile, `PLAN_LIMITS` retention row, D3 privacy mono strip).
c: guardrails ("We measured what actually scans" — authored SVG threshold plot;
precision: 160+ combinations across two campaigns) · API console (tabs, per-tab
server-highlighted, facts strip incl. real 404-indistinguishability) · comparison
(archetypes; rows link to feature pages) · built-in-the-open (repo + shiki excerpt of
real guardrail constants) · manifesto + infra strip (our facts only: 302+no-store
never 301 · KV in front of Postgres · retarget instant, ≤5min worst case · raw IPs
never stored; no unattributed third-party figures) · pricing teaser per pick.

**Shipped as planned, all three chunks board-reviewed on production before the next
began.** a: `8a490ab`. b: `565cfe9`, plus a same-day e2e fix `c362102` (a strict-mode
locator collision on the RetargetTheatre caption test, not a product bug — 32 of 33
other e2e tests passed on the first push). c: `d1cee95`, plus two follow-up commits:
`e303c03` (an e2e locator over-match, spec-only) and `9f2d784`, which is where a real,
flagged deck deviation landed — the guardrails plot's caption was reworded per
reviewer direction on the pushed diff, differing from copy deck v3's own verbatim
string (recorded in `docs/STATUS.md`'s T3c entry, not folded silently into "no
deviations"). `9f2d784` also carries a real product fix (the comparison table's
QRCDN column now leads the column order below `md`, so it's visible without
scrolling) alongside its e2e scoping fixes. Board round 5's live-device hero fixes
(iOS Safari `HUE_TINT` bug, pillar strip `hidden md:block`, `QrTile` padding, dropped
trailing period) landed folded into chunk b rather than as a separate commit, since
they touched files chunk b was already reasoning about — full detail in
`docs/guides/design-system.md`'s destination-identity-palette and hero-v4 sections,
per this file's own "carry the decision arc, not duplicate it" rule.

### T4 — Supporting surfaces
Pricing v2 (display h1, banded matrix + sticky headers, guarantee→band, FAQ→split,
air closing) · legal rhythm alignment · login value panel ≥lg · 404 on the system.

**Shipped exactly as planned (`393c9a4`).** No divergences — the landing stayed
untouched per the unit's own explicit scope boundary, and every one of the four
surfaces landed with the recipe named above (the pricing matrix's sticky-header
bug, found and fixed live during this unit, is implementation detail that lives in
`docs/STATUS.md`'s T4 entry, not a scope change).

### T5 — Docs ascent
Quickstart-first (five-minute copy-pasteable: mint key → create → scan → retarget)
above the comprehensive reference; params/errors tables from the data model; copy
buttons + highlighting throughout.

**Shipped exactly as planned (`92010a1`).** Reading the real `app/api/v1/**` route
handlers directly (rather than assuming from prior docs) surfaced four true API
behaviors that had never been documented anywhere before this unit: the public API's
slug lookup is case-sensitive while the redirect Worker matches case-insensitively
(D12) — tracked in this file's own P10 backlog note as of T7; `status` has no
`"expired"` value on the wire (it's a derived, dashboard-only label); `scanCount` can
lag up to an hour behind a real scan (the D8 hourly rollup) while `today.scans` is
live; and PATCH's response is a genuinely narrower shape than the full code object.
None required a handler change — all four are now documented for the first time,
which is the unit's whole point.

### T-F — Feature pages (two reviewed chunks)
`/features/dynamic-codes` · `/features/analytics` (chunk 1); `/features/brand-studio`
· `/features/access-controls` (chunk 2). Benefit hero → deep how-it-works → real
visuals → honest limits → scoped FAQ → CTA. Landing sections gain "Explore →"
doorways; teaser + page copy decked together; sitemap/OG/e2e grow (OG via the script).

**Shipped as planned, two chunks, each reviewed on production.** Chunk 1:
`9349308`. Chunk 2: `7daa7cc`, plus a same-day visual-consistency fix `b5d70cd`
(access-controls' guarantee band was missing the small-caps Eyebrow every sibling
guarantee band on the site carries — not a copy or truth-gate change). Every page
composes proven landing components with additive props rather than forking new
visual systems (`StateCards`' `layout`/`only` props, `ClosingSection`'s `title`/
`lede` props, `Playground`'s `embedded` prop) — the pattern this file's own T8
close formalizes in `docs/guides/design-system.md`. **Real divergence, found at
chunk 2 and fixed later at T7:** the copy deck's hero lede and S2 lede for
access-controls both described a code "scheduling" capability (a start date,
alongside expiry) that does not exist in the product — no `starts_at` column, no
UI input, no API field. Per the standing "a claim you cannot prove does not ship"
rule, both ledes were rewritten on the spot to describe only the real expiry
capability; `lib/pricing.ts`'s shared matrix row label ("Expiry, password &
scheduling") carried the same imprecision but was out of this chunk's scope to
edit, so `/features/access-controls`' own table locally overrode just that row's
label text (with a dated comment explaining why) rather than perpetuating the claim
onto a new page. T7 later fixed the shared label itself — see T7 below.

### T6 — Changelog · Status · OSS pack
`/changelog`: real ledger-curated entries, mono tags, RSS. `status.qrcdn.com`:
separate tiny CF Worker (independent failure domain from app AND redirect Worker),
request-time probes (apex 302+no-store contract, www 200, API auth-gate), honest
render, no fake uptime bars; DNS + route; never touches partyreel. OSS pack: designed
README (architecture map, escrow paragraph, hosted-is-the-product framing), LICENSE
(MIT), SECURITY.md, CONTRIBUTING, footer/developers repo links, STATUS + P10 reversal.

**Shipped exactly as planned (`aca586e`).** No substance deviations; three judgment
calls recorded at the code and in `docs/STATUS.md`: `wrangler.jsonc` (not the plan's
literal `wrangler.toml`, matching `workers/redirect`'s existing config format), a
real CI badge added to the README (the plan's own condition — "keep it only if the
URL is real" — read as permission to add one, since `.github/workflows/ci.yml`
genuinely exists), and the new footer links landing in the existing Legal column
rather than a new Resources column. `status.qrcdn.com`'s first live deploy also
found a real platform issue no planning session anticipated (two Workers on one
Cloudflare zone don't reach each other over the public internet by default) — fixed
with one compatibility flag; full writeup in `docs/STATUS.md`'s T6 entry.

### T-R — Blog + Help
Blog: `@next/mdx` scoped to blog (verify Turbopack support against bundled docs;
TSX-posts fallback), components mapped, RSS. Posts (real work only): decode-campaign
write-up · never-dies architecture (D2) · scan-privacy design (D3) · "Why QRCDN is
open source" (company-voiced principles). Help: ~10 task articles on the prose system
(create/retarget/pause · downgrade in plain words · password+expiry · bulk CSV · API
quickstart pointer · account deletion); categorized index; search deferred. Nav stays
lean (Features dropdown/index, Docs, Pricing, Blog, auth slot); footer becomes the
full resource map.

**Shipped `e615481`, plus a same-day content fix `c664380`. Two real, deliberate
divergences from the plan, both reasoned rather than assumed:**

1. **Typed-TSX posts shipped, not MDX** — the plan named MDX with typed-TSX as the
   documented fallback; the fallback is what shipped. `@next/mdx` was installed,
   wired into `next.config.ts`, and a throwaway `.mdx` page was built and compiled
   clean under Turbopack in a real `pnpm build` — proving the bundled Next 16 docs'
   caveat ("remark/rehype plugins without serializable options cannot yet be used
   with Turbopack") is narrower than "MDX doesn't work here." Reverted anyway,
   because the real fit problem was orthogonal to Turbopack: this repo has no
   `@tailwindcss/typography` and no prior MDX element-style mapping, so raw markdown
   output would render fully unstyled without hand-building a parallel typography
   layer duplicating what `Section`/`CodeBlock`/the type scale already give typed TSX
   for free — and `--container-prose` (65ch) was reserved back at T1b explicitly as
   "the future blog unit's measure," a standing signal this codebase expected typed
   components here. All deps/config/scratch files from the MDX trial were removed
   before shipping (`pnpm install --frozen-lockfile` reverified clean).
2. **The plan's own fact-check requirement (article 10, "account deletion") found
   and fixed two real content defects, one in a page this unit doesn't otherwise
   touch:** grepping for `deleteUser`/`/settings`/`/account` across the app found no
   self-serve deletion path reachable by a real signed-in user (`auth.admin.deleteUser`
   exists only in e2e teardown scripts) — help article 10 states that honestly (a
   request to hello@, handled by hand) instead of describing a button that doesn't
   exist. The same check found `app/(marketing)/privacy/page.tsx` claiming deletion
   works "from within the product or by writing to us," which is false; corrected to
   the honest single path in the same commit. A third, related finding shaped help
   article 8 ("What happens when I downgrade," the plan's own literal title): Stripe
   billing has never shipped (P8's own finding, still true at T8), so downgrading
   cannot happen yet at all. The article leads with that plainly rather than
   describing a self-serve flow that does not exist, then states the real policy
   commitment (D14) for when billing exists.

`c664380` fixed a smaller, later-caught defect: post 1 ("What actually scans")
originally claimed an active export block at the error threshold ("we warn at 0.395
and refuse at 0.412") that doesn't exist — export is warn-only, verified against
`controls-rail.tsx`/`code-actions.ts` directly. Fixed to "call it an error at 0.412,"
matching the post's own established vocabulary; the other three posts were swept for
the same class of overclaim and none were found.

### T7 — Product quick wins (deep redesign = named later phase)
api-keys free state → mini showcase (curl from `api-reference.ts`, `PLAN_LIMITS.pro`
rows, honest `/pricing` CTA) · `/codes`: header Create entry + per-row pause via
form-action server action (table stays server-rendered) · studio rail: two-cluster
grouping (Design / Content & output) + Export emphasis + zero-kit empty state if
reachable (verify).

**Shipped across three commits, not one — `067b3cf` (the four planned items plus
three riders), `addfd57` (a same-day mechanism fix for the first commit's own new
pause-toggle test, which failed CI's E2E run), and `e695670` (a founder production-
review round with three further fixes not folded into this file until now).**

All four planned items shipped. One implementation divergence from the plan's own
phrasing: `/codes`' per-row pause did **not** ship as a form-action server action —
that was the first of three mechanisms tried, and it (along with a `useActionState`
variant tried second) empirically failed to reliably apply the action's response to
the DOM; the third, shipped mechanism is a plain imperative client call to the same
`setCodePaused` action followed by a forced `window.location.reload()`. Full
mechanism-by-mechanism account: `components/codes/pause-toggle-button.tsx`'s own doc
comment and `docs/STATUS.md`'s T7 entry. **`067b3cf`'s own commit claimed CI-green
confidence in the second (`useActionState`) mechanism; the very next E2E run
contradicted that claim, twice, at two different lines — a race, not a fixed bug
with one cause.** Root-caused by direct local reproduction and fixed forward in
`addfd57` to the third mechanism, never reverted; recorded here as the expected
behavior this phase established (T7 itself, in `docs/STATUS.md`, calls out two
other spec claims caught false the same way), not as a failure to hide.

**Two riders close divergences this file already flagged elsewhere:** the T-F2
"scheduling" imprecision (above) is fixed at its source — `lib/pricing.ts`'s shared
matrix row label corrected to "Expiry & password," with `/features/access-controls`'
own local override retired now that the shared label agrees. **The third rider is
this phase's own standing-rule miss, found and fixed, not swept under anything:** a
47-violation em-dash sweep across product and public scan-facing copy (15 files —
`/auth/confirm`, `/p/[slug]`'s unlock form, `/codes`, the api-keys panel, the code
analytics panel, seven studio components, `lib/preview.ts`, `lib/pricing.ts`). The
no-em-dash rule was locked at T3a (copy deck v3) and enforced on marketing sections
at every unit since, but nothing had ever swept the product UI or the public
scan-facing pages against it — that gap is exactly what T7 closed, plus a new
standing regression guard (`lib/no-em-dash.test.ts`) so it can't reopen silently.
One found violation was deliberately left alone and recorded as a reasoned exemption
(`lib/guardrails-excerpt.ts`'s build-time invariant error message, never reachable by
a real visitor) — see T8 below for why that exemption boundary still needed one more
finding closed.

**`e695670` (review round 1, not previously logged in this file or fully in
`docs/STATUS.md`'s own T7 narrative — closed together with this unit):** the
founder's live production review of both themes at 1440/390 found three real defects,
none a redesign. HIGH: "Peak day" fabricated a date for a zero-scan range — a naive
`series.reduce(..., series[0])` needs a seed, and when every point is 0 scans nothing
ever beats it, so a brand-new account saw a real-looking date attached to zero scans.
Fixed with a single tested `peakDayFrom` function (`lib/analytics.ts`) returning
`day: null` when there's nothing real to report, replacing two independently-broken
copies of the same bug in `codes-overview-panel.tsx` and `code-analytics-panel.tsx`.
MEDIUM: the studio rail read "Ink" twice in a row under Colors (predates T7, carried
through the round's own regroup, fixed anyway) — the fill-mode toggle is now labeled
"Fill." LOW: `/api-keys`' "See pricing" button spanned the full card width, reading
as a form submit; now natural inline width, left-aligned with the rest of the card.

### T8 — Proof & close
e2e additions (h1 opacity guard · shiki-present · scanner-safety · new-surface
checks) · docs (design-system amendments, D16, STATUS ledger) · honest-content audit ·
full production sweep (both themes; 375/768/1024/1440) · board close review.

**Shipped (this unit).** Four of the five originally-planned e2e additions already
existed by the time this unit was scoped (h1-opacity, comparison, changelog+RSS, and
scanner-safety were all built incrementally across T1a/T3a/T3c/T6/T0, not held back
for T8) — verified directly against `apps/web/e2e/marketing.spec.ts` and
`auth-scanner-safety.spec.ts` before adding anything, so T8 did not re-add them. The
one real gap was proving shiki's highlighted markup actually reaches the browser in
the server response — closed with two new e2e tests (raw-HTML assertion + a
codified "zero client chunk" check), the first proven to fail against a temporarily
stubbed highlighter before being restored. D16 had already landed at T0, so this
unit's docs work was reconciliation (this file, `design-system.md`, `STATUS.md`) plus
the honest-content mechanical pass, rather than a new decision entry. One rider
folded in mid-unit, found by the founder's own full production sweep: the "Built in
the open" section's build-time excerpt of `packages/qr-engine/src/guardrails.ts`
rendered three em dashes to every visitor, sourced from that file's own doc comments
— fixed at the source (the excerpt itself must stay verbatim, so the fix could not
live in the excerpt logic) plus a hardened assertion closing the exact class of gap
that let it through T7's own sweep (a different package, only customer-facing by
passing through the excerpt). Full report: this unit's own final message to the
orchestrator.

## Verification bar (every unit)

Root `pnpm lint && pnpm typecheck && pnpm test` + `pnpm --filter web build` with the
`○ (Static)` audit → commit (playbook conventions) → push → CI+E2E watched by exact
SHA (poll ≥30s). T0 additionally proven by the scanner-safety spec and a real
new-address production sign-up. Every T3/T-F chunk reviewed live before the next.

This bar held for every unit in the phase, T0 through T8: no unit was ever declared
done on a red CI/E2E run for its own SHA, and every T3/T-F chunk got its live review
before the next chunk began. It was tested for real once, at T7 (above): the first
push's E2E run went red on its own new test, and the bar's response was to fix
forward to a green SHA and report the red run honestly rather than quietly
re-polling an older passing commit or loosening what "done" means.

// @qrcdn/shared — style JSON zod schema (v-tagged), slug utilities, and
// types shared between the web app and the redirect worker.
// Style schema lands in P1; slug utils in P3.

export const STYLE_SCHEMA_VERSION = 1;

# deps
node_modules/

# next.js
apps/web/.next/
apps/web/out/
next-env.d.ts

# builds
dist/
*.tsbuildinfo

# env & secrets
.env
.env.*
!.env.example

# supabase local
supabase/.branches
supabase/.temp

# wrangler
.wrangler/
.dev.vars

# vercel
.vercel/

# misc
.DS_Store
*.log
coverage/
test-results/
playwright-report/

# local agent state (belt-and-braces: often in a contributor's global ignore,
# enforced here so a collaborator clone is protected too)
.claude/settings.local.json

# stray root-level build output (apps/web/.next/ is covered above)
.next/

# scratch
scratch/
tmp/
temp/
